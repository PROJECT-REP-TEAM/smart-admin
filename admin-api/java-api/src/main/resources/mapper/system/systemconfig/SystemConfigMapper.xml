<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.smartadmin.module.system.systemconfig.SystemConfigDao">
    <update id="updateByKey">
        UPDATE t_system_config
        SET config_value = #{configValue}
        WHERE config_key = #{configKey}
    </update>

    <!-- 查询所有系统配置(分页) -->
    <select id="selectSystemSettingList" resultType="net.lab1024.smartadmin.module.system.systemconfig.domain.SystemConfigEntity">
        SELECT
            id,
            config_name,
            config_key,
            config_value,
            config_group,
            is_using,
            remark,
            create_time,
            update_time
        FROM
            t_system_config
        <where>
            <if test="queryDTO.key != null and queryDTO.key != ''">
                AND INSTR(config_key,#{queryDTO.key})
            </if>
            <if test="queryDTO.configGroup != null">
                AND INSTR(config_group,#{queryDTO.configGroup})
            </if>
        </where>
    </select>
    <select id="getListByGroup" resultType="net.lab1024.smartadmin.module.system.systemconfig.domain.SystemConfigEntity">
        SELECT
        id,
        config_name,
        config_key,
        config_value,
        config_group,
        is_using,
        remark,
        create_time,
        update_time
        FROM
        t_system_config where config_group= #{group}
    </select>
    <!-- 根据key查询获取数据 -->
    <select id="getByKey" resultType="net.lab1024.smartadmin.module.system.systemconfig.domain.SystemConfigEntity">
        SELECT
            id,
            config_name,
            config_key,
            config_value,
            config_group,
            is_using,
            remark,
            create_time,
            update_time
        FROM
            t_system_config
        WHERE
            config_key = #{key}
    </select>



    <select id="getByKeyExcludeId" resultType="net.lab1024.smartadmin.module.system.systemconfig.domain.SystemConfigEntity">
        SELECT
        id,
        config_name,
        config_key,
        config_value,
        config_group,
        is_using,
        remark,
        create_time,
        update_time
        FROM
        t_system_config
        WHERE
        config_key = #{key} and id &lt;&gt; #{excludeId}
    </select>

    <select id="selectByKeyAndGroup" resultType="net.lab1024.smartadmin.module.system.systemconfig.domain.SystemConfigEntity">
        SELECT
        id,
        config_name,
        config_key,
        config_value,
        config_group,
        is_using,
        remark,
        create_time,
        update_time
        FROM
        t_system_config
        WHERE  config_group = #{group} AND config_key =  #{configKey}
    </select>
</mapper>